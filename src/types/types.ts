export interface FormData {
    title: string;
    amount: number;
    currency: string;
    persons: string[];
}
  

export interface Participant {
    name: String;
    amountPaid: number;
    amountOwed: number;
}